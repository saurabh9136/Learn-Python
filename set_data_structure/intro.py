"""
set is the collection of unorderd/no index items
Each element in the set must be unique & immutable
"""

#*********** SET IS MUTABLE BUT SET ELEMENTS ARE IMMUTABLE *********************

# set is denoted by {} difference is here we need to pass only values compaing to dict
collection = {1, 2, 4, 5, "hello", "I", "am", "Saurabh", 22}

# to init the empty set need to use below syntax
collection_null = set()



print(collection)
print(type(collection_null))